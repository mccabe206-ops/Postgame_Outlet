# PGO automatic season updates

Approved by the user on September 9, 2026: after each game update model W/L; after the week ends update rankings and the next slate; refresh availability before kickoff, keep the existing T-60 forecast lock, and archive all original picks and accuracy.

Use the existing frozen September 9 postseason fit and its trained state-update rules. No fitting, new injury penalty, probability recalibration, or accuracy promotion is authorized by this operational change. Preserve all existing issued evidence bytes, including the after-lock Week 1 confidence annotation.

A scheduled GitHub Actions job checks every 15 minutes (subject to runner/provider delays), with manual dispatch available. Capture ESPN final status/scores with exact season/week/team/kickoff identity and raw provenance. Actual ties are a separate T; missing or no-winner picks are not wins/losses. Results are idempotent and append-only; a changed accepted final is flagged for review rather than silently overwritten. Existing editions and the new weekly series retain separate records.

Once all scheduled games in a week have verified final results, fetch current nflverse schedule, team statistics, QB statistics, roster and depth snapshots. Require complete admitted game coverage and agreement with final scores. Reconstruct the trained team accumulators from pinned historical data once, then replay completed current-season games with the original rules and frozen coefficients. Create a new dated rankings/predictions edition only after the checks pass; show a delay if statistics or identities are unavailable. The scoring-total heuristic and probability calibration remain frozen, with their limits disclosed.

Check availability for upcoming games within 24 hours and before T-60. Save observations, official final inactives when available, source timestamps and explicit unknown coverage. Confirmed missing expected QBs block that conditional prediction unless a sourced replacement can be calculated. Non-QB information remains annotation-only until a separately tested model supports numerical adjustments. A delayed scheduled job never rewrites a prediction after cutoff. Retain every earlier pregame revision and its sources. Confidence integers are allocated once per week; later pre-lock QB-driven revisions retain assigned points.

Publish the current week, ranking movement, model W/L/T, per-game scores and confidence points, availability/freshness and prior-week archives in the existing theme. All old Week 1 evidence remains directly accessible. No separate service or dependency is needed. All 2026 regular-season weeks are supported; a completed season stops without inventing next-season fixtures.

Acceptance: deterministic offline tests covering nonfinal/final/tie, duplicate and corrected results, week completeness including byes/postponements, missing stats, once-only season transition, future-data exclusion, T-60 immutability and delayed jobs, probability sums/confidence uniqueness, archival hash validation and reader-friendly display. Run the existing full suite, a real-source no-refit refresh, a scheduled-job dispatch, public-byte verification, and desktop/mobile review. Model remains EXPERIMENTAL / HOLD; operational tests are not scientific qualification.

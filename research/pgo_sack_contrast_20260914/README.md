# Fixed QB/team sack contrast experiment

Result: **further-study screen FAIL; stability screen PASS; no promotion**.
See [the complete result](report.md) and the standalone read-only
`verify_result.py` checker. Run attempt 01 completed exactly nine fits once.

This isolated research candidate tests stronger shrinkage of the difference
between two overlapping sack-avoidance weights. It preserves both features.
It has no production import, live input fetch, rating refresh, or serving hook.

The user authorized the exact experiment on September 14, 2026. The previously
sealed design remains unchanged: SHA-256
`87004c8e946118a2f70c89d04d4aba8aecc5ab8a7e67cdfddb311bee2778e273`.
Its original DESIGN ONLY wording records its earlier preparation stage; the
new execution authorization is separate. It permits eight chronological
evaluation fits and one final historical fit, each once, with no promotion.

Preflight attempt 01 stopped with zero candidate fits: the historical receipt
stored the pinned helper under an absolute path, while the first check assumed
a relative key. The helper bytes were unchanged. The repair resolves exactly
one normalized path suffix and retains the required historical hash. The full
failure receipt remains in `preflight-attempt01`.

Preflight attempt 02 passed all source, chronological-fold, training-only
preprocessing and saved-control replay checks. The 10 accompanying tests do
not fit models. Its manifest is
`02d9fad8015c64203c329079394cb28b9eced94f597d577d697c3e16efd53bb1`.

The executable uses only the pinned historical feature file. It rejects
changed bytes, IDs, feature order, malformed or nonfinite values and invalid
saved scales. Each optimizer call gets a durable start record; any failure
preserves the attempt and stops it. A completed final fit is not an additional
evaluation fold. Frozen artifacts use byte-preserving Git attributes.

All historical seasons were previously inspected. Historical source timing and
actual-starter limitations remain REVIEW REQUIRED. Results can support further
research, not an accuracy claim for current public forecasts. Non-QB injury
weights, probabilities, totals, ATS choices, confidence points, and 2026 team
rankings are outside this experiment. Its prospective phase is NOT STARTED.

**Verdict: REVISE BEFORE IMPLEMENTATION.** The scientific design is largely coherent and the ten formulas do reproduce the older v2 meanings with every departure declared. Two blocking problems are procedural and structural, not statistical. I could not verify any SHA-256 in this review because only Read, Glob and Grep were available.

## Blocking

- **Lines 3-6 and 214-216: the charter's "before candidate feature construction" status is already false, and no correction record exists.** The candidate directory contains `adapter.py`, `train.py`, `test_candidate.py`, and `preflight-20260908/` holding only `run-start.json`. That receipt records kind FEATURE_PREPARATION_NO_FIT, status STARTED_INCOMPLETE, started 2026-09-08T17:28:43Z, under this exact charter hash. A construction attempt was spent and did not finish. Revision: add a dated correction record naming preflight-20260908 as an incomplete preparation with no fit and no artifacts, and reword line 3-4 to "before any completed preparation or fit". Note that `train.py` hardcodes the charter hash at line 19, so any charter revision must update that pin before the next attempt.

- **Lines 179-187 and 210-213: the "optional" current diagnostic is wired as a hard dependency of the preparation receipt, and as implemented it cannot complete.** The trainer runs the current diagnostic inside `prepare()` at `train.py:92`, after the full historical walk. The adapter calls the issued comparison helper with two arguments at `adapter.py:189`, but `pgo_forecast_corrected.py:75` defines it with three required parameters. That raises before any artifact is written, the historical features are discarded, and every retry consumes a new exclusive directory. This is the most likely cause of the incomplete preflight. Revision: the charter must make the historical preparation receipt and the current diagnostic separate receipts, so a diagnostic failure is recorded without invalidating base parity and coverage evidence.

## Important

- **Line 107 "never an inferred zero" misdescribes the inherited role history.** In the base walker at `pgo_challenger.py:2325-2330`, every ACT-rostered player with no matched snap row receives a recorded 0.0 share. That includes game-day inactives, snap rows unmatched by PFR id or name, and source gaps. Reword: the adapter adds no new zeros, but the base context records a zero share for any rostered player without a matched snap row, and that zero is inherited and disclosed. Also disclose at lines 104-109 that snap attribution uses the base name fallback at `pgo_challenger.py:2302-2304`, so "never guess by name" at line 85 applies only to the adapter's join.

- **Lines 25-35: the seven pins are not enforced by anything described.** The trainer's `pins()` hashes the corrected run manifest, the September 8 manifest, the prior run manifest and the corrected code set. Nothing hashes `research/pgo_v1/sources.lock.json`, the three old v2 files, or the v2 backtest. Either require the runner and the independent verifier to hash and record them, or state that they are reviewer-manual provenance pins only.

- **Lines 236-239: the screen is ambiguous against existing code and nobody is assigned to compute it.** The trainer writes metrics and intervals only. The existing helper at `audit_model.py:455-466` applies four checks, including weeks 1-4 not worse. The charter lists three. State which rule is authoritative, name the independent verifier as the component that computes it, and add one sentence that intervals versus secondary controls at line 232 are descriptive and cannot substitute for the primary screen.

- **Lines 83-84 and 179-183: current-period identity collisions are unchecked.** The collision-aware identity uses the historical `colliding_gsis` set from the portable context. The 2026 week 1 ACT rows are never checked for a GSIS appearing on more than one team or with multiple smart ids, so a 2026-only collision would silently share one player's snap history with another. Require the same collision test the base applies at `pgo_challenger.py:2532-2542`, failing on any current-period collision.

- **Line 85 versus line 170-171: ID-less ACT rows are a stop condition that the coverage design cannot express.** The base silently skips roster rows with neither GSIS nor PFR id at `pgo_challenger.py:2158-2159`. The adapter's identity-set comparison would abort on such a row, and its coverage field for unmatched identities is hardcoded to zero at `adapter.py:84`, so "denominators must include unmatched identities" is vacuous. State explicitly that an ID-less ACT row stops the run, and require the pre-fit raw inventory to count them. Also define "raw ACT rows" at line 168 as pre-collapse file rows, since rows reaching the hook are already collapsed at `pgo_challenger.py:2482-2500`; record the global collapse count from the walker's identity-resolution receipt.

- **Lines 159-165: the 2016 vintage shift is disclosed but has no acceptance rule.** Blank draft is roughly 63 percent in 2016 versus 25-31 percent elsewhere, and blank maps to zero. Home-minus-away differencing cancels a season-common shift only if blank rates are roughly uniform across teams within 2016. Require the coverage receipt to show the 2016 blank-draft rate by team, and state that the shift is an accepted inherited limitation so nobody later argues for dropping 2016, which line 176-177 already forbids.

- **Line 143: missing flags carry no information under symmetric augmentation.** Because each mirrored pair shares the same flag with negated targets, the flag coefficients are identically zero, which the corrected verifier already asserts at `verify.py:110`. So a missing matchup difference is imputed to the augmented median, which is zero. Say this plainly. It also means the unit-wide missing-experience rule at lines 121-124 simply zeroes the rookie-capital difference for that game, which is coherent but should be stated.

## Minor

- Line 96-98: "UTC date of kickoff" means an Eastern evening kickoff ages players on the next UTC date. This matches old v2 exactly, since it used the UTC datetime's date, but declare it and add a UTC-boundary birthday case to the fixture at line 193.
- Line 129: after home-minus-away differencing, the 27 centering cancels in the linear term and only affects the squared term. Note it to avoid over-reading the name.
- Line 99: the read-only inventory counted absent birth dates but not malformed or out-of-range ones. Add that count to the pre-fit inventory so a stop is anticipated rather than discovered mid-walk.
- Line 224: the eight actual ties match the corrected metrics file.

## Verified as coherent

The ten formulas match old v2 `_unit_roster_features` and `_qb_feature` at v2 `pgo_challenger.py:2340-2441`, with the availability weighting, recorded-QB selection, missing-experience rule and invalid-draft failures all declared at lines 90-125. Position aliases and unit sets match v2 lines 66-74. The current walker prepares every same-kickoff game before any update at `pgo_challenger.py:2019-2082`, so the hook reads pre-batch snap history and the perturbation test at lines 194-199 is implementable. Symmetric augmentation negates every finite field including venue, rest and the signed squared term, alpha 200 on doubled rows equals 100 per original observation, and validation rows are scored once unmirrored. The bootstrap helper's sign, eight-block resampling and percentile endpoints match lines 229-233. The corrected reference MAE at line 222 matches the saved metrics file, the matched table carries the corrected and comparator columns named at lines 222-223, and the 66-of-67 source inventory rule matches the corrected trainer.

Approval, once revised, would cover adapter implementation and pre-fit checks only. It would not cover fitting, adoption, forecast issuance or publication.